"""
### Snapshot notebooks — frozen, run-only records.

Empty upstream by default: snapshots are produced by the dashboard's 📸
freeze (moving a workspace notebook here on the user's fork) and arrive
upstream via PR. Never edited — a snapshot is valued precisely because it
does not change.

spec: [docs/architecture/notebook.md](../architecture/notebook.md)
"""
